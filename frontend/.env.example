NUXT_PUBLIC_API_BASE=http://localhost:8080
NUXT_PUBLIC_POLL_INTERVAL_MS=15000

# Server-only (Nuxt BFF). Must match backend ADMIN_API_KEY.
# Never prefix with NUXT_PUBLIC_ — that would leak to the browser.
NUXT_ADMIN_API_KEY=dev-admin-key-change-me
NUXT_API_BASE_SERVER=http://localhost:8080
